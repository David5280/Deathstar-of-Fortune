class Wheel {
  constructor(values) {
    this.values = values
    this.currentValues = []
  }
  selectCurrentValues() {
    //choose the values for the currentValues
  }
  spin() {
    //randomly returns a currentValue
  }
}